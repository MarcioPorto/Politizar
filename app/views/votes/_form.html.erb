
<%= simple_form_for(@vote) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :description %>
    <%= f.association :bill %>
    <%= f.association :representative %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
